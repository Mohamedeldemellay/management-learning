import data.string;

import java.io.File;
import java.io.FileNotFoundException;
import java.util.Scanner;

class Main {
    public static void main(String[] args) throws FileNotFoundException {
        File selectedfile = new File ("student-data.txt");
             Scanner scanner = new Scanner(selectedfile);

    private static void While (Scanner.hasNext()) {
        string data = scanner.nextLine().replace()
        }
    }

 /* Sorry, but i can’t undersatand.
    i will study again, and i will deliver in a few days.
  */